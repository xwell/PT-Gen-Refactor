# PT-Gen-Refactor 环境变量配置示例
# 一键部署时会显示这些配置项，请根据需要填写
# 注意：空值会导致表单验证失败，可选字段请保留默认值或填写实际值

# API 访问密钥（可选，留空表示无需密钥即可访问）
# 设置后需在请求中添加 ?key={API_KEY}
API_KEY=optional

# TMDB API 密钥（可选）
# 用于获取 TMDB 资源，获取地址：https://www.themoviedb.org/settings/api
TMDB_API_KEY=optional

# 豆瓣 Cookie（可选）
# 用于访问登录可见资源
DOUBAN_COOKIE=optional

# QQ Cookie（可选）
# 用于访问腾讯视频资源
QQ_COOKIE=optional

# 启用缓存（可选）
# 设置为 true 启用 R2 缓存
ENABLED_CACHE=true
